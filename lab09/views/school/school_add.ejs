<!DOCTYPE html>
<html>
<head>
    <title>School - Add</title>
</head>
<body>
<h1>Add New School</h1>
<form action="/school/insert" method="get">
    <br />
    School Name: <input name="school_name" type="text" />
    <br />
    Address:
    <select id="address_id" name="address_id">
        <% for(var i=0; address.length > i; i++) { %>
        <option value="<%= addrress[i].address_id %>"><%= address[i].street + '. ' + address[i].zip_code %></option>
        <% } %>
    </select>
    <br />
    <input type="submit" />

</form>
</body>
</html>